#search fot a given string in a file:
##-i to ignore Case
grep -i "the" demo_file


#Print the matched line, along with the 3 lines after it:
grep -A 3 -i "example" demo_file.txt

#Search for a given string in all files recursively
grep -r "walid" /*
